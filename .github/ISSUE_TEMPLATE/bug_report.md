---
name: Bug report
about: Something did not behave as documented
labels: bug
---

**Which tool?** `find_opportunities` / `check_eligibility` / `fill_application` / `list_learned_portals` / `track_deadlines`

**What happened?**

**The machine-readable error** (paste the `{ code, message, recovery_hint }` block):

**Did it touch a submit/pay control?** If yes, this is a P0 — please also follow SECURITY.md.

**Environment**
- ApplyOnce version:
- `webcmd --version`:
- `webcmd doctor` output:
- Node version:
- Portal (and whether the layout looks recently changed):

**Redact personal data before posting.** Logs are masked by default; double-check anyway.
